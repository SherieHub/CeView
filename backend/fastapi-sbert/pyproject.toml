[project]
name = "ceview-fastapi-sbert"
version = "0.1.0"
description = "CeView SBERT microservice — Modules 1, 3 & 4 (classification, content generation, analytics)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.7.0",
    "python-multipart>=0.0.9",
    "httpx>=0.27.0",
    "google-genai>=0.3.0",
]

[tool.uv]
package = false
