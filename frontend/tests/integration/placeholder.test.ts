import { describe, expect, it } from 'vitest';

/**
 * Placeholder so `npm run test:integration` (and CI's "Integration tests"
 * step) has something to run before any screen card adds real
 * integration-level tests (e.g. full App render + route navigation). Delete
 * once a real integration test lands in this directory.
 */
describe('tests/integration placeholder', () => {
  it('is a no-op until a real integration test lands here', () => {
    expect(true).toBe(true);
  });
});
